# Spring Framework深度学习第四阶段：Bean生命周期与依赖注入

## 1. 概述

在Spring框架中，Bean的生命周期管理和依赖注入是IoC容器的核心功能。这一阶段主要关注Bean的创建、初始化、使用和销毁的完整生命周期，以及Spring如何实现依赖注入的各种机制。

## 2. 核心概念和组件

### 2.1 Bean生命周期

Bean的生命周期从实例化开始，到销毁结束，包括多个阶段和回调点。Spring提供了多种方式来干预Bean的生命周期，包括：

- 实现特定接口（如InitializingBean, DisposableBean）
- 使用JSR-250注解（@PostConstruct, @PreDestroy）
- 在XML配置中指定init-method和destroy-method
- 使用@Bean注解的initMethod和destroyMethod属性

### 2.2 依赖注入方式

Spring支持多种依赖注入方式：

- 构造函数注入 
- Setter方法注入
- 字段注入（通过@Autowired）
- 方法注入
- 接口回调注入（通过Aware接口）

### 2.3 BeanPostProcessor

BeanPostProcessor是Spring提供的一个非常重要的扩展点，它允许我们在Bean初始化前后进行自定义处理。

### 2.4 循环依赖

Spring通过三级缓存机制解决了单例Bean的循环依赖问题，这是Spring IoC容器设计中的一个亮点。

## 3. 关键流程分析

### 3.1 Bean获取与创建流程

#### 3.1.1 Bean获取入口

Bean的获取流程始于AbstractBeanFactory的getBean方法，最终委托给doGetBean方法处理：

```java
@Override
public Object getBean(String name) throws BeansException {
    return doGetBean(name, null, null, false);
}

@Override
public <T> T getBean(String name, Class<T> requiredType) throws BeansException {
    return doGetBean(name, requiredType, null, false);
}

@Override
public Object getBean(String name, Object... args) throws BeansException {
    return doGetBean(name, null, args, false);
}
```

#### 3.1.2 doGetBean方法详解

doGetBean方法是Bean获取的核心方法，负责处理从缓存获取或创建新Bean的完整流程：

```java
@SuppressWarnings("unchecked")
protected <T> T doGetBean(
        String name, @Nullable Class<T> requiredType, @Nullable Object[] args, boolean typeCheckOnly)
        throws BeansException {

    // 转换Bean名称，处理别名和工厂引用前缀
    String beanName = transformedBeanName(name);
    Object beanInstance;

    // 1. 尝试从单例缓存中获取已存在的Bean实例
    Object sharedInstance = getSingleton(beanName);
    if (sharedInstance != null && args == null) {
        if (logger.isTraceEnabled()) {
            if (isSingletonCurrentlyInCreation(beanName)) {
                logger.trace("Returning eagerly cached instance of singleton bean '" + beanName +
                        "' that is not fully initialized yet - a consequence of a circular reference");
            }
            else {
                logger.trace("Returning cached instance of singleton bean '" + beanName + "'");
            }
        }
        // 处理FactoryBean的情况，获取FactoryBean创建的对象
        beanInstance = getObjectForBeanInstance(sharedInstance, name, beanName, null);
    }

    else {
        // 2. 如果缓存中没有，开始创建Bean的流程
        // 检查是否正在创建原型Bean，防止循环引用
        if (isPrototypeCurrentlyInCreation(beanName)) {
            throw new BeanCurrentlyInCreationException(beanName);
        }

        // 检查父容器中是否存在Bean定义
        BeanFactory parentBeanFactory = getParentBeanFactory();
        if (parentBeanFactory != null && !containsBeanDefinition(beanName)) {
            // 委托给父容器处理
            String nameToLookup = originalBeanName(name);
            if (parentBeanFactory instanceof AbstractBeanFactory abf) {
                return abf.doGetBean(nameToLookup, requiredType, args, typeCheckOnly);
            }
            else if (args != null) {
                return (T) parentBeanFactory.getBean(nameToLookup, args);
            }
            else if (requiredType != null) {
                return parentBeanFactory.getBean(nameToLookup, requiredType);
            }
            else {
                return (T) parentBeanFactory.getBean(nameToLookup);
            }
        }

        // 标记Bean为已创建
        if (!typeCheckOnly) {
            markBeanAsCreated(beanName);
        }

        // 3. 开始创建Bean实例
        StartupStep beanCreation = this.applicationStartup.start("spring.beans.instantiate")
                .tag("beanName", name);
        try {
            if (requiredType != null) {
                beanCreation.tag("beanType", requiredType::toString);
            }
            
            // 获取合并后的Bean定义
            RootBeanDefinition mbd = getMergedLocalBeanDefinition(beanName);
            checkMergedBeanDefinition(mbd, beanName, args);

            // 4. 处理依赖关系
            String[] dependsOn = mbd.getDependsOn();
            if (dependsOn != null) {
                for (String dep : dependsOn) {
                    if (isDependent(beanName, dep)) {
                        throw new BeanCreationException(mbd.getResourceDescription(), beanName,
                                "Circular depends-on relationship between '" + beanName + "' and '" + dep + "'");
                    }
                    registerDependentBean(dep, beanName);
                    try {
                        // 递归获取依赖的Bean
                        getBean(dep);
                    }
                    catch (NoSuchBeanDefinitionException ex) {
                        throw new BeanCreationException(mbd.getResourceDescription(), beanName,
                                "'" + beanName + "' depends on missing bean '" + dep + "'", ex);
                    }
                }
            }

            // 5. 根据作用域创建Bean实例
            if (mbd.isSingleton()) {
                // 单例Bean创建
                sharedInstance = getSingleton(beanName, () -> {
                    try {
                        return createBean(beanName, mbd, args);
                    }
                    catch (BeansException ex) {
                        // 创建失败时从单例缓存中移除
                        destroySingleton(beanName);
                        throw ex;
                    }
                });
                beanInstance = getObjectForBeanInstance(sharedInstance, name, beanName, mbd);
            }
            else if (mbd.isPrototype()) {
                // 原型Bean创建
                Object prototypeInstance = null;
                try {
                    beforePrototypeCreation(beanName);
                    prototypeInstance = createBean(beanName, mbd, args);
                }
                finally {
                    afterPrototypeCreation(beanName);
                }
                beanInstance = getObjectForBeanInstance(prototypeInstance, name, beanName, mbd);
            }
            else {
                // 自定义作用域Bean创建
                String scopeName = mbd.getScope();
                if (!StringUtils.hasLength(scopeName)) {
                    throw new IllegalStateException("No scope name defined for bean '" + beanName + "'");
                }
                Scope scope = this.scopes.get(scopeName);
                if (scope == null) {
                    throw new IllegalStateException("No Scope registered for scope name '" + scopeName + "'");
                }
                try {
                    Object scopedInstance = scope.get(beanName, () -> {
                        beforePrototypeCreation(beanName);
                        try {
                            return createBean(beanName, mbd, args);
                        }
                        finally {
                            afterPrototypeCreation(beanName);
                        }
                    });
                    beanInstance = getObjectForBeanInstance(scopedInstance, name, beanName, mbd);
                }
                catch (IllegalStateException ex) {
                    throw new ScopeNotActiveException(beanName, scopeName, ex);
                }
            }
        }
        catch (BeansException ex) {
            cleanupAfterBeanCreationFailure(beanName);
            throw ex;
        }
        finally {
            beanCreation.end();
            if (!isCacheBeanMetadata()) {
                clearMergedBeanDefinition(beanName);
            }
        }
    }

    // 6. 适配Bean实例到所需的类型
    return adaptBeanInstance(name, beanInstance, requiredType);
}
```

#### 3.1.3 getObjectForBeanInstance方法

getObjectForBeanInstance方法处理从缓存或新创建的Bean实例，特别是处理FactoryBean的情况：

```java
protected Object getObjectForBeanInstance(
        Object beanInstance, String name, String beanName, @Nullable RootBeanDefinition mbd) {

    // 处理工厂解引用（&前缀）
    if (BeanFactoryUtils.isFactoryDereference(name)) {
        if (beanInstance instanceof NullBean) {
            return beanInstance;
        }
        if (!(beanInstance instanceof FactoryBean)) {
            throw new BeanIsNotAFactoryException(beanName, beanInstance.getClass());
        }
        if (mbd != null) {
            mbd.isFactoryBean = true;
        }
        // 返回FactoryBean本身
        return beanInstance;
    }

    // 如果不是FactoryBean，直接返回Bean实例
    if (!(beanInstance instanceof FactoryBean<?> factoryBean)) {
        return beanInstance;
    }

    // 处理FactoryBean，获取FactoryBean创建的对象
    Object object = null;
    if (mbd != null) {
        mbd.isFactoryBean = true;
    }
    else {
        object = getCachedObjectForFactoryBean(beanName);
    }
    
    if (object == null) {
        // 从FactoryBean获取对象实例
        if (mbd == null && containsBeanDefinition(beanName)) {
            mbd = getMergedLocalBeanDefinition(beanName);
        }
        boolean synthetic = (mbd != null && mbd.isSynthetic());
        object = getObjectFromFactoryBean(factoryBean, beanName, !synthetic);
    }
    return object;
}
```

#### 3.1.4 doCreateBean方法详解

Bean的实际创建流程在AbstractAutowireCapableBeanFactory的doCreateBean方法中实现：

```java
protected Object doCreateBean(String beanName, RootBeanDefinition mbd, @Nullable Object[] args)
    throws BeanCreationException {
    
    // 1. 实例化Bean
    BeanWrapper instanceWrapper = null;
    if (mbd.isSingleton()) {
        instanceWrapper = this.factoryBeanInstanceCache.remove(beanName);
    }
    if (instanceWrapper == null) {
        instanceWrapper = createBeanInstance(beanName, mbd, args);
    }
    Object bean = instanceWrapper.getWrappedInstance();
    Class<?> beanType = instanceWrapper.getWrappedClass();
    if (beanType != NullBean.class) {
        mbd.resolvedTargetType = beanType;
    }

    // 2. 允许后置处理器修改合并的Bean定义
    synchronized (mbd.postProcessingLock) {
        if (!mbd.postProcessed) {
            try {
                applyMergedBeanDefinitionPostProcessors(mbd, beanType, beanName);
            }
            catch (Throwable ex) {
                throw new BeanCreationException(mbd.getResourceDescription(), beanName,
                        "Post-processing of merged bean definition failed", ex);
            }
            mbd.markAsPostProcessed();
        }
    }

    // 3. 处理循环依赖：提前暴露未完全初始化的Bean
    boolean earlySingletonExposure = (mbd.isSingleton() && this.allowCircularReferences &&
        isSingletonCurrentlyInCreation(beanName));
    if (earlySingletonExposure) {
        if (logger.isTraceEnabled()) {
            logger.trace("Eagerly caching bean '" + beanName +
                    "' to allow for resolving potential circular references");
        }
        // 将Bean工厂加入到三级缓存中
        addSingletonFactory(beanName, () -> getEarlyBeanReference(beanName, mbd, bean));
    }
    
    // 4. 初始化Bean实例
    Object exposedObject = bean;
    try {
        // 4.1 属性填充（依赖注入）
        populateBean(beanName, mbd, instanceWrapper);
        
        // 4.2 初始化Bean
        exposedObject = initializeBean(beanName, exposedObject, mbd);
    }
    catch (Throwable ex) {
        if (ex instanceof BeanCreationException bce && beanName.equals(bce.getBeanName())) {
            throw bce;
        }
        else {
            throw new BeanCreationException(mbd.getResourceDescription(), beanName, ex.getMessage(), ex);
        }
    }
    
    // 5. 处理循环依赖
    if (earlySingletonExposure) {
        Object earlySingletonReference = getSingleton(beanName, false);
        if (earlySingletonReference != null) {
            if (exposedObject == bean) {
                exposedObject = earlySingletonReference;
            }
            else if (!this.allowRawInjectionDespiteWrapping && hasDependentBean(beanName)) {
                String[] dependentBeans = getDependentBeans(beanName);
                Set<String> actualDependentBeans = CollectionUtils.newLinkedHashSet(dependentBeans.length);
                for (String dependentBean : dependentBeans) {
                    if (!removeSingletonIfCreatedForTypeCheckOnly(dependentBean)) {
                        actualDependentBeans.add(dependentBean);
                    }
                }
                if (!actualDependentBeans.isEmpty()) {
                    throw new BeanCurrentlyInCreationException(beanName,
                            "Bean with name '" + beanName + "' has been injected into other beans [" +
                            StringUtils.collectionToCommaDelimitedString(actualDependentBeans) +
                            "] in its raw version as part of a circular reference, but has eventually been " +
                            "wrapped. This means that said other beans do not use the final version of the " +
                            "bean. This is often the result of over-eager type matching - consider using " +
                            "'getBeanNamesForType' with the 'allowEagerInit' flag turned off, for example.");
                }
            }
        }
    }
    
    // 6. 注册销毁回调
    try {
        registerDisposableBeanIfNecessary(beanName, bean, mbd);
    }
    catch (BeanDefinitionValidationException ex) {
        throw new BeanCreationException(
                mbd.getResourceDescription(), beanName, "Invalid destruction signature", ex);
    }
    
    return exposedObject;
}
```

### 3.2 Bean初始化流程

Bean的初始化流程在initializeBean方法中实现：

```java
protected Object initializeBean(String beanName, Object bean, RootBeanDefinition mbd) {
    // 1. 调用Aware接口方法
    invokeAwareMethods(beanName, bean);
    
    Object wrappedBean = bean;
    if (mbd == null || !mbd.isSynthetic()) {
        // 2. 应用BeanPostProcessors的postProcessBeforeInitialization方法
        wrappedBean = applyBeanPostProcessorsBeforeInitialization(wrappedBean, beanName);
    }
    
    try {
        // 3. 调用初始化方法
        invokeInitMethods(beanName, wrappedBean, mbd);
    }
    catch (Throwable ex) {
        throw new BeanCreationException(...);
    }
    
    if (mbd == null || !mbd.isSynthetic()) {
        // 4. 应用BeanPostProcessors的postProcessAfterInitialization方法
        wrappedBean = applyBeanPostProcessorsAfterInitialization(wrappedBean, beanName);
    }
    
    return wrappedBean;
}
```

### 3.3 依赖注入流程

依赖注入主要在populateBean方法中实现：

```java
protected void populateBean(String beanName, RootBeanDefinition mbd, @Nullable BeanWrapper bw) {
    // 1. 检查实例和BeanWrapper
    if (bw == null) {
        if (mbd.hasPropertyValues()) {
            throw new BeanCreationException(...);
        }
        else {
            // 没有属性可填充，直接返回
            return;
        }
    }
    
    // 2. 给InstantiationAwareBeanPostProcessors机会修改属性值
    boolean continueWithPropertyPopulation = true;
    if (!mbd.isSynthetic() && hasInstantiationAwareBeanPostProcessors()) {
        for (BeanPostProcessor bp : getBeanPostProcessors()) {
            if (bp instanceof InstantiationAwareBeanPostProcessor) {
                InstantiationAwareBeanPostProcessor ibp = (InstantiationAwareBeanPostProcessor) bp;
                if (!ibp.postProcessAfterInstantiation(bw.getWrappedInstance(), beanName)) {
                    continueWithPropertyPopulation = false;
                    break;
                }
            }
        }
    }
    
    if (!continueWithPropertyPopulation) {
        return;
    }
    
    // 3. 获取属性值
    PropertyValues pvs = (mbd.hasPropertyValues() ? mbd.getPropertyValues() : null);
    
    // 4. 自动注入模式处理
    if (mbd.getResolvedAutowireMode() == RootBeanDefinition.AUTOWIRE_BY_NAME ||
        mbd.getResolvedAutowireMode() == RootBeanDefinition.AUTOWIRE_BY_TYPE) {
        MutablePropertyValues newPvs = new MutablePropertyValues(pvs);
        
        // 根据名称自动注入
        if (mbd.getResolvedAutowireMode() == RootBeanDefinition.AUTOWIRE_BY_NAME) {
            autowireByName(beanName, mbd, bw, newPvs);
        }
        
        // 根据类型自动注入
        if (mbd.getResolvedAutowireMode() == RootBeanDefinition.AUTOWIRE_BY_TYPE) {
            autowireByType(beanName, mbd, bw, newPvs);
        }
        
        pvs = newPvs;
    }
    
    // 5. 检查是否有InstantiationAwareBeanPostProcessors需要处理属性
    boolean hasInstAwareBpps = hasInstantiationAwareBeanPostProcessors();
    boolean needsDepCheck = (mbd.getDependencyCheck() != RootBeanDefinition.DEPENDENCY_CHECK_NONE);
    
    PropertyDescriptor[] filteredPds = null;
    if (hasInstAwareBpps) {
        if (pvs == null) {
            pvs = mbd.getPropertyValues();
        }
        for (BeanPostProcessor bp : getBeanPostProcessors()) {
            if (bp instanceof InstantiationAwareBeanPostProcessor) {
                InstantiationAwareBeanPostProcessor ibp = (InstantiationAwareBeanPostProcessor) bp;
                PropertyValues pvsToUse = ibp.postProcessProperties(pvs, bw.getWrappedInstance(), beanName);
                if (pvsToUse == null) {
                    if (filteredPds == null) {
                        filteredPds = filterPropertyDescriptorsForDependencyCheck(bw, mbd.allowCaching);
                    }
                    pvsToUse = ibp.postProcessPropertyValues(pvs, filteredPds, bw.getWrappedInstance(), beanName);
                    if (pvsToUse == null) {
                        return;
                    }
                }
                pvs = pvsToUse;
            }
        }
    }
    
    if (needsDepCheck) {
        if (filteredPds == null) {
            filteredPds = filterPropertyDescriptorsForDependencyCheck(bw, mbd.allowCaching);
        }
        checkDependencies(beanName, mbd, filteredPds, pvs);
    }
    
    // 6. 应用属性值
    if (pvs != null) {
        applyPropertyValues(beanName, mbd, bw, pvs);
    }
}
```

### 3.4 循环依赖解决机制

Spring通过三级缓存解决循环依赖问题：

1. **一级缓存（singletonObjects）**：存放完全初始化的单例Bean
2. **二级缓存（earlySingletonObjects）**：存放提前暴露的未完全初始化的Bean
3. **三级缓存（singletonFactories）**：存放Bean工厂，用于生成提前暴露的Bean代理

```java
// 获取单例Bean的方法
protected Object getSingleton(String beanName, boolean allowEarlyReference) {
    // 1. 从一级缓存获取完全初始化的Bean
    Object singletonObject = this.singletonObjects.get(beanName);
    
    // 2. 如果一级缓存中没有，且Bean正在创建中
    if (singletonObject == null && isSingletonCurrentlyInCreation(beanName)) {
        synchronized (this.singletonObjects) {
            // 3. 从二级缓存获取提前暴露的Bean
            singletonObject = this.earlySingletonObjects.get(beanName);
            if (singletonObject == null && allowEarlyReference) {
                // 4. 从三级缓存获取Bean工厂
                ObjectFactory<?> singletonFactory = this.singletonFactories.get(beanName);
                if (singletonFactory != null) {
                    // 5. 通过Bean工厂获取Bean
                    singletonObject = singletonFactory.getObject();
                    // 6. 将Bean放入二级缓存
                    this.earlySingletonObjects.put(beanName, singletonObject);
                    // 7. 从三级缓存中移除
                    this.singletonFactories.remove(beanName);
                }
            }
        }
    }
    return singletonObject;
}
```

## 4. 代码示例

### 4.1 Bean生命周期回调示例

```java
// 示例Bean类，展示各种生命周期回调
public class LifecycleBean implements InitializingBean, DisposableBean, BeanNameAware, ApplicationContextAware {
    
    private String name;
    private ApplicationContext applicationContext;
    private String beanName;
    
    // 构造函数
    public LifecycleBean() {
        System.out.println("1. 构造函数执行");
    }
    
    // Setter方法（依赖注入）
    public void setName(String name) {
        System.out.println("2. Setter方法执行，注入name属性: " + name);
        this.name = name;
    }
    
    // BeanNameAware接口方法
    @Override
    public void setBeanName(String name) {
        System.out.println("3. BeanNameAware.setBeanName()执行，Bean名称: " + name);
        this.beanName = name;
    }
    
    // ApplicationContextAware接口方法
    @Override
    public void setApplicationContext(ApplicationContext applicationContext) throws BeansException {
        System.out.println("4. ApplicationContextAware.setApplicationContext()执行");
        this.applicationContext = applicationContext;
    }
    
    // @PostConstruct注解方法
    @PostConstruct
    public void postConstruct() {
        System.out.println("6. @PostConstruct注解方法执行");
    }
    
    // InitializingBean接口方法
    @Override
    public void afterPropertiesSet() throws Exception {
        System.out.println("7. InitializingBean.afterPropertiesSet()执行");
    }
    
    // 自定义初始化方法
    public void initMethod() {
        System.out.println("8. 自定义init-method执行");
    }
    
    // @PreDestroy注解方法
    @PreDestroy
    public void preDestroy() {
        System.out.println("销毁时: @PreDestroy注解方法执行");
    }
    
    // DisposableBean接口方法
    @Override
    public void destroy() throws Exception {
        System.out.println("销毁时: DisposableBean.destroy()执行");
    }
    
    // 自定义销毁方法
    public void destroyMethod() {
        System.out.println("销毁时: 自定义destroy-method执行");
    }
    
    // 业务方法
    public void doSomething() {
        System.out.println("执行业务方法，name: " + name);
    }
}
```

### 4.2 自定义BeanPostProcessor示例

```java
// 自定义BeanPostProcessor，在Bean初始化前后添加日志
@Component
public class LoggingBeanPostProcessor implements BeanPostProcessor, Ordered {
    
    @Override
    public Object postProcessBeforeInitialization(Object bean, String beanName) throws BeansException {
        System.out.println("Bean初始化前: " + beanName + " -> " + bean.getClass().getName());
        return bean;
    }
    
    @Override
    public Object postProcessAfterInitialization(Object bean, String beanName) throws BeansException {
        System.out.println("Bean初始化后: " + beanName + " -> " + bean.getClass().getName());
        return bean;
    }
    
    @Override
    public int getOrder() {
        return Ordered.LOWEST_PRECEDENCE;
    }
}
```

### 4.3 循环依赖示例

```java
// 示例1：构造函数循环依赖（无法解决）
@Component
public class BeanA {
    private final BeanB beanB;
    
    public BeanA(BeanB beanB) {
        this.beanB = beanB;
    }
}

@Component
public class BeanB {
    private final BeanA beanA;
    
    public BeanB(BeanA beanA) {
        this.beanA = beanA;
    }
}

// 示例2：属性循环依赖（可以解决）
@Component
public class BeanC {
    @Autowired
    private BeanD beanD;
    
    public void doSomething() {
        System.out.println("BeanC doing something");
        beanD.doSomething();
    }
}

@Component
public class BeanD {
    @Autowired
    private BeanC beanC;
    
    public void doSomething() {
        System.out.println("BeanD doing something");
    }
}
```

### 4.4 Bean获取与创建详细示例

```java
// 演示Bean获取与创建的完整流程
@Component
public class BeanCreationDemo implements ApplicationContextAware {
    
    private ApplicationContext applicationContext;
    
    @Override
    public void setApplicationContext(ApplicationContext applicationContext) throws BeansException {
        this.applicationContext = applicationContext;
    }
    
    public void demonstrateBeanCreation() {
        System.out.println("=== 演示Bean获取与创建流程 ===");
        
        // 1. 从容器获取Bean
        System.out.println("1. 开始获取BeanA");
        BeanA beanA = applicationContext.getBean(BeanA.class);
        System.out.println("获取到的BeanA: " + beanA);
        
        // 2. 获取FactoryBean
        System.out.println("\n2. 开始获取FactoryBean创建的对象");
        MyService myService = applicationContext.getBean("myFactoryBean", MyService.class);
        System.out.println("获取到的Service: " + myService);
        
        // 3. 获取FactoryBean本身
        System.out.println("\n3. 开始获取FactoryBean本身");
        MyFactoryBean factoryBean = applicationContext.getBean("&myFactoryBean", MyFactoryBean.class);
        System.out.println("获取到的FactoryBean: " + factoryBean);
        
        // 4. 演示循环依赖
        System.out.println("\n4. 演示循环依赖的解决");
        BeanC beanC = applicationContext.getBean(BeanC.class);
        beanC.doSomething();
    }
}

// 示例BeanA，展示创建过程
@Component
public class BeanA implements BeanNameAware, InitializingBean {
    
    private String beanName;
    
    public BeanA() {
        System.out.println("BeanA: 构造函数执行");
    }
    
    @Override
    public void setBeanName(String name) {
        System.out.println("BeanA: setBeanName执行，名称为: " + name);
        this.beanName = name;
    }
    
    @Override
    public void afterPropertiesSet() throws Exception {
        System.out.println("BeanA: afterPropertiesSet执行");
    }
    
    @Override
    public String toString() {
        return "BeanA[" + beanName + "]";
    }
}
```

## 5. Bean获取与创建的关键点

### 5.1 Bean获取的核心步骤

1. **名称转换与解析**：
   - 处理别名（alias）
   - 解析工厂引用前缀（&）
   - 转换为规范名称

2. **缓存检查**：
   - 首先检查一级缓存（singletonObjects）
   - 如果不存在，检查二级缓存（earlySingletonObjects）
   - 如果仍然不存在，检查三级缓存（singletonFactories）

3. **父容器委托**：
   - 如果当前容器不存在Bean定义，委托给父容器处理
   - 递归向上查找，直到找到或抛出异常

4. **依赖处理**：
   - 处理depends-on属性，确保依赖的Bean先创建
   - 检查循环依赖，防止无限递归

5. **实例化**：
   - 根据作用域（singleton/prototype/custom）创建实例
   - 应用实例化前后的后置处理器

6. **初始化**：
   - 属性填充（依赖注入）
   - 调用初始化方法
   - 应用初始化前后的后置处理器

7. **类型适配**：
   - 如果指定了requiredType，进行类型转换
   - 确保返回的对象符合期望的类型

### 5.2 循环依赖解决机制

1. **检测循环依赖**：
   - 通过isSingletonCurrentlyInCreation标记正在创建的Bean
   - 检测到循环时抛出BeanCurrentlyInCreationException

2. **提前暴露机制**：
   - 在实例化后，提前将Bean工厂放入三级缓存
   - 允许其他Bean引用尚未完全初始化的Bean

3. **三级缓存的作用**：
   - 一级缓存：存放完全初始化的Bean
   - 二级缓存：存放提前暴露的Bean
   - 三级缓存：存放创建Bean的工厂

4. **注入时机**：
   - Setter注入：支持循环依赖
   - 构造函数注入：不支持循环依赖

## 6. 总结

第四阶段的核心是理解Spring如何管理Bean的完整生命周期以及如何实现依赖注入。这些机制是Spring IoC容器的核心功能，也是理解Spring框架工作原理的关键。

通过学习Bean的生命周期，我们了解到Spring提供了多种方式来干预Bean的创建和初始化过程，包括接口回调、注解和配置方法等。通过学习依赖注入，我们了解到Spring支持多种注入方式，并且通过三级缓存机制巧妙地解决了循环依赖问题。

BeanPostProcessor是Spring框架中非常重要的扩展点，它允许我们在Bean初始化的前后进行自定义处理，是Spring实现AOP、事务管理等功能的基础。

## 6. 与前后阶段的关联

### 与前一阶段的联系

本阶段建立在第三阶段对容器初始化与刷新机制的理解之上，利用这些基础知识来理解Bean的创建和初始化过程。

### 为后一阶段奠定的基础

1. 理解了Bean的生命周期，为学习AOP代理机制做准备
2. 熟悉了依赖注入的实现，为理解事务管理中的依赖注入做准备
3. 了解了BeanPostProcessor机制，为理解AOP中的后置处理器做准备